export default function SupervisorPage() {
  return (
    <div>
      <h1>Supervisor Dashboard</h1>
      <p>Single page with all supervisor functionality</p>
      {/* Here would have overview, approvals, reports, basic settings, etc. */}
    </div>
  );
} 
